const initialState = {
    user: undefined,
    token: undefined
};

export default function registration (state = initialState, action) {
    const {type} = action;
    switch (type) {
    default:
        return state;
    }
}
